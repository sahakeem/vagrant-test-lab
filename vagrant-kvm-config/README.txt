This vagrant config file includes:

NOTE: This vagrant file is set to use KVM/Libvirt as a hypervisor

A complete CentOS ready to deploy vagrant file
* Inline script with user creation
* Static IP assignment
* Configurable ram/cpu resources for vagrant boxes. (The default is 512MB, my configuration is set to 3GB of ram per box) 
